
pub fn permute(stg:String,pocket:String) {
    
    if stg.len() == 0 {
        println!("{}",pocket)
    }else {
        for i in 0..stg.len() {
            let letter = &stg[0..i];
            let front=&stg[0..i];

            
        }
    }
}